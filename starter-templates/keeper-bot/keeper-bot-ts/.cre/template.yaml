kind: starter-template
id: keeper-bot-ts
projectDir: .
title: "Keeper Bot (TypeScript)"
description: "Cron-based smart contract maintenance — read state, check condition, execute if needed."
language: typescript
category: automation
tags:
  - keeper
  - upkeep
  - cron
  - on-chain-read
  - on-chain-write
workflows:
  - dir: my-workflow
postInit: |
  Run `cd my-workflow && bun install && cd ..` then simulate with `cre workflow simulate my-workflow`. A demo KeeperConsumer contract is pre-deployed on Sepolia. See README.md for details.
