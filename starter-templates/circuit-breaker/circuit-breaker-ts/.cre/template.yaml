kind: starter-template
id: circuit-breaker-ts
projectDir: .
title: "Circuit Breaker (TypeScript)"
description: "Monitor on-chain events for anomalies, automatically pause contracts when thresholds are breached."
language: typescript
category: risk-safety
tags:
  - circuit-breaker
  - risk
  - safety
  - pause
  - log-trigger
  - cron
  - on-chain-read
  - on-chain-write
workflows:
  - dir: my-workflow
postInit: |
  Run `cd my-workflow && bun install && cd ..` then simulate with `cre workflow simulate my-workflow`. A demo ProtocolWithBreaker contract is pre-deployed on Sepolia. See README.md for details.
