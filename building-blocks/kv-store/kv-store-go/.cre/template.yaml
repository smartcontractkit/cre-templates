kind: building-block
id: kv-store-go
projectDir: .
title: "KV Store (Go)"
description: "Read, increment, and write a counter in AWS S3 using SigV4-signed HTTP requests."
language: go
category: workflow
solutions:
  - defi-vault-operations
capabilities:
  - cron
  - http
  - secrets
tags:
  - storage
  - key-value
networks:
  - ethereum-testnet-sepolia
  - ethereum-mainnet
workflows:
  - dir: my-workflow
postInit: |
  Update secrets.yaml with your AWS credentials before running.
